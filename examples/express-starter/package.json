{
  "name": "zustand-multiplayer-express-starter",
  "description": "Express.js Todo App example for Zustand Multiplayer Middleware",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "start": "npm run build && node ./bin/www",
    "dev": "npm run build && nodemon ./bin/www",
    "build": "node scripts/build-client.js"
  },
  "dependencies": {
    "@hpkv/zustand-multiplayer": "^0.3.0",
    "cors": "^2.8.5",
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "zustand": "^5.0.3"
  },
  "devDependencies": {
    "esbuild": "^0.19.0",
    "nodemon": "^3.0.1"
  }
}
